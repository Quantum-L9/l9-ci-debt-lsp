# Fixture: GHA workflow where the Final Decision job is missing an install step.
# This file should trigger CI-DEPS-002 diagnostic.
# Source pattern: cryptoxdog/l9-ci-debt-resolver/references/classification-rules.md#p-003
name: ai-review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  analyze:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: ${{ github.workspace }}
    steps:
      - uses: actions/checkout@v4
      - name: Install deps
        run: pip install pyyaml jsonschema pydantic
      - name: Run analysis
        run: python tools/review/analyze.py

  final-decision:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: ${{ github.workspace }}
    needs: [analyze]
    steps:
      - uses: actions/checkout@v4
      # MISSING: Install deps step
      - name: Make final decision
        run: python tools/review/aggregate.py
